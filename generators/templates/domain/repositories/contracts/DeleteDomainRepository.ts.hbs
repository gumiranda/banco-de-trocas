import { Query } from "shared/types";
import { HttpResponse } from "shared/libs/infra";

export interface Delete{{pascalCase name}}Repository {
  delete{{pascalCase name}}(query: Query): Promise<HttpResponse | null>;
}
