import { Query } from "shared/types";
import { {{pascalCase name}}Paginated } from "../../{{pascalCase name}}Entity";

export interface Load{{pascalCase name}}ByPageRepository {
  load{{pascalCase name}}ByPage(query: Query): Promise<{{pascalCase name}}Paginated | null>;
}
