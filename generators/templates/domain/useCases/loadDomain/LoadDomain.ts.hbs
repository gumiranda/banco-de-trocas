import { Query } from "@/shared/types";
import { HttpResponse } from "@/shared/libs/infra";
import { Load{{pascalCase name}}Repository } from "../../repositories";

export type Load{{pascalCase name}} = (query: Query) => Promise<HttpResponse | null>;
export type Load{{pascalCase name}}Signature = (
  load{{pascalCase name}}: Load{{pascalCase name}}Repository
) => Load{{pascalCase name}};
export const load{{pascalCase name}}: Load{{pascalCase name}}Signature =
  (load{{pascalCase name}}Repository: Load{{pascalCase name}}Repository) => async (query: Query) => {
    return load{{pascalCase name}}Repository.load{{pascalCase name}}(query);
  };
